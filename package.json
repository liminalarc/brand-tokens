{
  "name": "brand-tokens",
  "version": "0.5.0",
  "description": "Brand design tokens (color, typography, radius, shadow) — light + dark, framework-agnostic. Single source of truth across apps.",
  "license": "UNLICENSED",
  "type": "module",
  "files": [
    "dist",
    "tokens.json",
    "DESIGN-SYSTEM.md",
    "CHANGELOG.md"
  ],
  "main": "./dist/tokens.js",
  "types": "./dist/tokens.d.ts",
  "exports": {
    ".": {
      "types": "./dist/tokens.d.ts",
      "default": "./dist/tokens.js"
    },
    "./css": "./dist/tokens.css",
    "./scss": "./dist/tokens.scss",
    "./components": "./dist/components.css",
    "./tokens.json": "./tokens.json"
  },
  "scripts": {
    "build": "node build.mjs",
    "audit": "node contrast-audit.mjs",
    "prepare": "node build.mjs"
  }
}
